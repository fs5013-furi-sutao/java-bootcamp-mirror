---
title: 07. Scoop のインストール方法
section: 01.イントロ
slug: /intro/07-how-to-install-scoop
order: 7
---

# 07. Scoop のインストール方法

Scoop のインストール方法は、公式サイトに記載がある。インストール自体は、コマンド 1 つでできるようになっている

## PowerShell の起動

コマンドの実行には、PowerShell が必要なので、PowerShell を開きます。

Win キーを押した後、` powershell ` と入力し、検索結果に PowerShell が表示されたら、Enter キーを押します。
これで PowerShell が起動します。

## 実行ポリシーの変更

まず、ユーザの実行ポリシーを以下のコマンドで `RemoteSigned` にします。

```console
Set-ExecutionPolicy RemoteSigned -scope CurrentUser
```

インターネットからダウンロードされたスクリプトおよび構成ファイルが、
信頼された発行元かどうかを確認し、良いと認められれば実行されるポリシーです。

## Scoop のインストール

以下のコマンドで、Scoop のインストールを実行します。

```console
Invoke-Expression (New-Object System.Net.WebClient).DownloadString('https://get.scoop.sh')
```

上記のコマンドを短縮したバージョンが以下のコマンドになります。

```console
iwr -useb get.scoop.sh | iex
```

コマンド実行後、エラーなく処理が完了すれば、Scoop がインストールできているはずです。
次のコマンドでインストールできているかの確認ができます。

```console
scoop --version
```

インストールできていれば、次のような実行結果になります。

```
Current Scoop version:
#=> c50706a6 (HEAD -> master, origin/master, origin/HEAD) scoop: Update to version 0.13.7
```

今後 Scoop でアプリをインストールするには、` git ` と ` 7zip ` が必要です。

` git ` は、Scoop の仕組みを構築するために必要で、` 7zip ` は圧縮ファイルを展開するために必要です。
` git ` さえインストールすれば、Scoop が自動的に ` 7zip ` もインストールしてくれるので、
以下のコマンドで Git をインストールします。

```console
scoop install git
```

Scoop のインストールは以上です。
